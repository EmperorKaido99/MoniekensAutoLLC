// Navbar — top navigation bar with logo, user avatar, and mobile menu toggle
export default function Navbar() {
  return <nav className="flex items-center justify-between px-4 py-3 border-b">Navbar</nav>;
}
