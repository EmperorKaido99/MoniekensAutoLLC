// PDF utilities — builds PDF buffers from quote and document templates
export async function buildQuotePdf(data: Record<string, unknown>): Promise<Buffer> {
  // TODO: implement with a PDF library such as pdf-lib or puppeteer
  return Buffer.alloc(0);
}
